
services:
  postgres:
    image: postgres:16.2
    container_name: db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: hipoteca
      POSTGRES_DB: petscare_db
    volumes:
      - ./data:/var/lib/postgresql/data
    ports:
      - 5432:5432
